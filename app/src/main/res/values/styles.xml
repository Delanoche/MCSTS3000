<resources>

    <style name="Theme.MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/mcts_green</item>
        <item name="colorPrimaryDark">@color/mcts_green_dark</item>
        <item name="colorAccent">@color/mcts_yellow</item>
        <!--<item name="android:background">@android:color/white</item>-->
        <item name="android:windowBackground">@android:color/white</item>
        <!--<item name="android:colorBackground">@android:color/white</item>-->
    </style>

    <style name="Theme.ActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/mcts_green</item>
        <item name="colorPrimaryDark">@color/mcts_green_dark</item>
        <item name="colorAccent">@color/mcts_yellow</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.MyTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>


</resources>
